// file: internal/models/models.go
// package: models
//
// 放一些通用数据模型（此工程很小，仅做简单的组织）。
package models

// 暂时预留模型文件（如果工程扩大，可把共享结构体放在这里）。
// 当前项目使用 internal/dedup.FileMeta 作为主要结构体（为了避免循环依赖，models 中现在为空）。
